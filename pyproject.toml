[project]
name = "emerald-herald-tools"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = []

[project.optional-dependencies]
export = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
